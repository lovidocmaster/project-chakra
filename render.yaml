services:
  - type: worker
    name: chakra-live-trader
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python live_paper_trader.py
    autoDeploy: true
