services:
  - type: worker
    name: chakra-live-trader
    runtime: python
    plan: starter
    pythonVersion: "3.11.9"
    buildCommand: "echo no build needed - live trader uses only Python stdlib"
    startCommand: python live_paper_trader.py
    autoDeploy: true
